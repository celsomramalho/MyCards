{
  "name": "mycards",
  "version": "2.0.0",
  "private": true,
  "description": "PWA com uma coleção de jogos e placares de cartas.",
  "scripts": {
    "dev": "python -m http.server 4174 --bind 127.0.0.1",
    "start": "python -m http.server 4174 --bind 127.0.0.1"
  }
}
